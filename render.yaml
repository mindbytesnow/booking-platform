services:
  - type: web
    name: booking-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        value: change-me
      - key: PGSSLMODE
        value: require
